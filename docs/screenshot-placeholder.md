# Screenshot needed

Before publishing, replace this file with `screenshot.png`:

1. `npm run dev`, open http://localhost:4321 on a 1280×800 viewport.
2. Capture the full first screen (hero + start of services).
3. Save as `docs/screenshot.png` (PNG, ~1280×800, < 500 KB).

The README and the Astro Themes catalog submission both use this image.
